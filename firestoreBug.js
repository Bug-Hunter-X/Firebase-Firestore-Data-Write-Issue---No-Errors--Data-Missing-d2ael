In a Firebase project, I encountered an issue where data wasn't being written to Firestore despite seemingly correct code. The write operation didn't throw any errors, and the console logs showed the data being sent correctly. However, the data wasn't appearing in the Firestore database.